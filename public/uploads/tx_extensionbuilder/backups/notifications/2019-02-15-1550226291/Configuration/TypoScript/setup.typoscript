
plugin.tx_notifications_notification {
    view {
        templateRootPaths.0 = EXT:{extension.extensionKey}/Resources/Private/Templates/
        templateRootPaths.1 = {$plugin.tx_notifications_notification.view.templateRootPath}
        partialRootPaths.0 = EXT:notifications/Resources/Private/Partials/
        partialRootPaths.1 = {$plugin.tx_notifications_notification.view.partialRootPath}
        layoutRootPaths.0 = EXT:notifications/Resources/Private/Layouts/
        layoutRootPaths.1 = {$plugin.tx_notifications_notification.view.layoutRootPath}
    }
    persistence {
        storagePid = {$plugin.tx_notifications_notification.persistence.storagePid}
        #recursive = 1
    }
    features {
        #skipDefaultArguments = 1
        # if set to 1, the enable fields are ignored in BE context
        ignoreAllEnableFieldsInBe = 0
        # Should be on by default, but can be disabled if all action in the plugin are uncached
        requireCHashArgumentForActionArguments = 1
    }
    mvc {
        #callDefaultActionIfActionCantBeResolved = 1
    }
}

plugin.tx_notifications_newsletter {
    view {
        templateRootPaths.0 = EXT:{extension.extensionKey}/Resources/Private/Templates/
        templateRootPaths.1 = {$plugin.tx_notifications_newsletter.view.templateRootPath}
        partialRootPaths.0 = EXT:notifications/Resources/Private/Partials/
        partialRootPaths.1 = {$plugin.tx_notifications_newsletter.view.partialRootPath}
        layoutRootPaths.0 = EXT:notifications/Resources/Private/Layouts/
        layoutRootPaths.1 = {$plugin.tx_notifications_newsletter.view.layoutRootPath}
    }
    persistence {
        storagePid = {$plugin.tx_notifications_newsletter.persistence.storagePid}
        #recursive = 1
    }
    features {
        #skipDefaultArguments = 1
        # if set to 1, the enable fields are ignored in BE context
        ignoreAllEnableFieldsInBe = 0
        # Should be on by default, but can be disabled if all action in the plugin are uncached
        requireCHashArgumentForActionArguments = 1
    }
    mvc {
        #callDefaultActionIfActionCantBeResolved = 1
    }
}

plugin.tx_notifications_offer {
    view {
        templateRootPaths.0 = EXT:{extension.extensionKey}/Resources/Private/Templates/
        templateRootPaths.1 = {$plugin.tx_notifications_offer.view.templateRootPath}
        partialRootPaths.0 = EXT:notifications/Resources/Private/Partials/
        partialRootPaths.1 = {$plugin.tx_notifications_offer.view.partialRootPath}
        layoutRootPaths.0 = EXT:notifications/Resources/Private/Layouts/
        layoutRootPaths.1 = {$plugin.tx_notifications_offer.view.layoutRootPath}
    }
    persistence {
        storagePid = {$plugin.tx_notifications_offer.persistence.storagePid}
        #recursive = 1
    }
    features {
        #skipDefaultArguments = 1
        # if set to 1, the enable fields are ignored in BE context
        ignoreAllEnableFieldsInBe = 0
        # Should be on by default, but can be disabled if all action in the plugin are uncached
        requireCHashArgumentForActionArguments = 1
    }
    mvc {
        #callDefaultActionIfActionCantBeResolved = 1
    }
}

# these classes are only used in auto-generated templates
plugin.tx_notifications._CSS_DEFAULT_STYLE (
    textarea.f3-form-error {
        background-color:#FF9F9F;
        border: 1px #FF0000 solid;
    }

    input.f3-form-error {
        background-color:#FF9F9F;
        border: 1px #FF0000 solid;
    }

    .tx-notifications table {
        border-collapse:separate;
        border-spacing:10px;
    }

    .tx-notifications table th {
        font-weight:bold;
    }

    .tx-notifications table td {
        vertical-align:top;
    }

    .typo3-messages .message-error {
        color:red;
    }

    .typo3-messages .message-ok {
        color:green;
    }
)
